# 客户端（桌面监控程序 main.py）专用依赖
# 说明：客户端零模型、无 onnxruntime / pytesseract（视觉兜底走服务端 OCR）。
# 截屏改用 Windows GDI（ctypes+numpy），无第三方截屏库依赖。
numpy>=1.26.0
opencv-python>=4.8.0
psutil>=5.9.0
requests>=2.31.0
cryptography>=43.0.0
sqlalchemy==2.0.36
python-json-logger==2.0.7
# 可选：浏览器地址栏 URL 读取（Windows UIA）。未安装时分类器自动退回纯标题判定。
comtypes>=1.4.5
